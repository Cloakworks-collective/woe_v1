# Progress

**Phase: playable vertical slice, fully local. `pnpm dev` → http://localhost:3000 → found an empire and play.**

## Working now (verified in-browser end to end)
- **Pure engine** (`lib/engine/`, 67 tests): tick economy (starvation-before-production, tax, merc defection, race × statecraft × hall × unrest modifiers, RP banking + Collegium gates), daily reset (growth, wall penalty, housing cap, scattering w/ <500 exemption), all economy commands, **combat** (4 phases, 4 modes, walls/escalade/counters, merc-first deaths, XP bands incl. verified refusal + revenge exemption, loot w/ storage protection, bombard), **espionage** (5 ops, lodge gating, executions), **market** (anonymous cheapest-first book, 5% burn), **clans** (3× ledger, churn rules, wars w/ tribute + truce), **victory** (score fn, #1 hold-clocks, winner banner).
- **Server** (`lib/server/`): JSON file world store (Supabase stand-in), seeded world (8 bots + a clan + Bazaar asks), wall-clock catch-up ticks, `/api/tick` (cron target) + `/api/cmd/[name]` (the cmd:* protocol) + UI server actions all sharing one pipeline, dev cookie auth + login/create-empire flow, dev time controls (+1 turn / +1 day when no CRON_SECRET).
- **UI**: all 11 pages live in the Travian style — Command View (decrees: tax/surrender/bank, Collegium, advisors, Chronicle), Buildings (+repairs, foundry ladder), Train, Troops (equip/mercs/siege gear/rest), Attack (war room + battle reports), Rankings (victory clocks + clan ladder), Bazaar, Spy/Scout, Clan, Forum (era/clan/DM), Login.
- **Supabase**: full schema + RLS written (`supabase/migrations/0001_init.sql`) — awaiting a cloud project (user credentials) to swap file store → Postgres, cookie → Auth, refresh → Realtime.
- **Sim** (`pnpm sim`): economy pacing (Village→Town ~day 38, ~820 pop by day 60) + combat matchup/luck tables.
- **Premium**: Stripe checkout (test-mode cards, incl. keyless emulator) → Royal Charter flag → the Steward each tick (build/research queues + standing orders), premium-gated across engine, pipeline, and UI.
- **Terminal client + API**: realm-token bearer auth, JSON read endpoints (join/state/rankings/market/battle), and `pnpm woe` — a colorful zero-dep CLI playing the same empire as the browser.

## Recently landed
- **Direct troop training + typed/tiered mercenaries** (dropped the "warrior" middle step): peasants now train **straight into** footmen/archers/cavalry at light/medium/heavy — one command `trainTroops(type, tier, count)` (folds the old 50-gold muster levy into each troop's cost; still gated by trainer≥tier + Forge≥tier + a free Muster Hall bed). `dischargeTroops(type, tier, count)` sends a soldier home directly (gear lost; needs a free Hearthstead bed and the 30% guard line). The whole `Player.warriors` pool + `equipTroops`/`disbandTroops`/`dischargeWarriors`/`trainWarriors` are gone. **Mercenaries are now a typed force** (`ArmyState.mercenaries: MercForce` = footmen/archers/cavalry × tiers): hired via `buyMercs(type, tier, count)` for gold only, **requiring the same buildings** as regulars (per user's call), priced `500 × race × tier-mult × (1−wonder)`, still capped 25% of regulars, upkeep-or-defect, zero ranking. **Combat rewrite**: merc groups fight in their own arm's phase at their tier but **die before the matching regulars** (front line per arm; `isMerc` groups, targeted merc-first within a category; proportional phases hit everyone). Legacy saves migrated on load (`normalizePlayer`: flat mercs → light-footman sellswords, old warriors → idle peasants). Touched engine (types/commands/combat/tick/score/reports/eraRecords/steward/onboarding/espionage), pipeline+world, UI (train Muster = spies/scouts only; troops = train/discharge/typed-merc-hire; steward order builder; Census/BattleReportPanel/AdvisorAlerts/SideNav/TourGuide; guide/premium copy), CLI (`troop`/`discharge`/`mercs <type> <tier> <n>`), plugin skill, and all specs (buildings/overview/combat/economy/architecture/premium). Verified end-to-end through the live HTTP pipeline (train/discharge/hire + tier gating). `tsc` clean, **111 tests pass**.
- **Visual pass II — game-like identity everywhere** (less text, more parchment/pixel): **pixel resource tokens** (7 new PixelLab assets in `public/art/resources/`: food/wood/stone/ore/gold/turns, via `<ResIcon>`) now replace the emoji in the **top ResourceBar**, the **Market** tables (resource column + gold prices), and cost readouts — resources read identically on every page. The **Command dashboard** replaced its two `dl.kv` stat text-lists with a **framed `StatTile` grid** (icon + label + value, gold-token income tiles) + pixel **`Meter`** bars (walls / stamina / army-XP with real caps `STAMINA.MAX` / 0–100 / integrity). **Guide** chapters each gained a floated framed illustration (`.guide-illo`, reusing building/unit art) — breaks the 21-paragraph wall. **Spy** panels show `units/spy`+`units/scout` art; **Clan** page got a new **heraldic crest** (`public/art/clan/crest.png`) header + member race-portraits; **Advisors** already rendered all 4 councillor portraits. New reusable components: `ResIcon`, `StatTile`, `Meter` (+ `.stat-grid`/`.stat-tile`/`.meter`/`.guide-illo`/`.crest-head` CSS). Verified in-browser on Command (tokens+dashboard) & Guide; `tsc` clean, 89 tests. **Next:** snapshot composition charts (army/workers/research — reuse `PriceChart` SVG), Command trend charts (needs an hourly sampler), Forum avatars.
- **Visual pass I — pixel art on the text-heavy pages** (making it read like a game, not a spreadsheet): **race portraits** now sit beside every name on the **Rankings ladder** and the **Attack war-room** target list (framed `Art path="races/…"`, new `.race-cell`/`.race-avatar` CSS). **Pixel tone-emblems** now head every row of the **Chronicle, World News, and Annals** feeds — 7 new 64px PixelLab glyphs in `public/art/tones/` (war=crossed swords, shadow=dagger, danger=skull, growth=wheat, trade=coins, clan=shield, info=scroll) via the `<ToneGlyph tone>` component (keyed off the existing `eventTone` classifier; renders nothing for unglyphed tones like "crown"). Existing infra leaned on: `Art` (crisp pixel-scaled), `PriceChart` (parchment SVG line chart — the pattern for future charts), ~53 prior PixelLab assets. Verified in-browser on all four pages; `tsc` clean, 89 tests. **Next visual opportunities** (audited, not yet built): snapshot charts (army/worker/research composition — no new data), Command trend charts (treasury/pop/food — needs an hourly sampler like `priceHistory`), event-tone SVG icons elsewhere, clan crests.
- **Elder Ages — the FULL historical record, in tables, grouped by victory era**: `lib/lore/elderAges.ts` uses a generic table model (`ElderTable[]` per age) with the complete leaderboards transcribed for **all 35 ages** (2005–2013) — Greatest Rulers, Strongest Clans/Empires, Champions of the Realms (each feat's champion + epithet), Lords & Ladies, Non-Battle Titles, Richest Attacks/Raids, Bloodiest Attacks, Greatest Wars, Greatest Feuds, Greatest Empires. Ages 15–35 were parsed from the original chronicles HTML by a one-off script (winners resolved via the AGE-WINNERS tag, not just rank; cross-checked against each next age's naming). `components/ElderAges.tsx` renders every table (parchment `.tbl`, right-aligned numerics, horizontal scroll). Chronicles split into the original's **four era groups** (`CHRONICLE_GROUPS`), all now populated: Champions (1–11, top player wins) · Empires (12–14, top empire wins) · Crown & Banner (15–31, both crowned — winning empire + "Foremost ruler" line) · Conquest (32–35, top empire wins by battle). **Split into subpages for load** (35 ages × ~10 tables was too heavy on one page): `/annals` shows only a light index of link-cards (`ElderAgesIndex`, grouped by era, no tables); the full leaderboards for one age render on `/annals/age/[age]` (`ElderAgeDetail`, with ← prev / All ages / next → pager). The top-nav "📚 Annals ▾" is a `<details>` dropdown → "The Annals — this age" + the four eras (anchor-linked to the index sections). The archive is complete.
- **The Annals — a grand world chronicle that's sealed for good at each age's end** (inspired by the original WoE "chronicles"): a new global event log (`world.chronicle`) records the realm's significant public deeds — the **crown changing hands** (crownHolderId tracked in updateVictoryClocks), **clan wars declared/won**, **castles sacked** (successful sieges w/ gold looted), and the **victory** that ends the age — each tone-coloured + time-stamped via `pushChronicle`. New **/annals** page ("The Annals of the Age") shows the live feed plus **Sealed Ages** accordions (winner + final top-10 ladder + entries). At era end, `eraReset()` now **seals the age into `world.chronicleArchive[]`** (carried across every reset — "published for good") and opens the next age's annals; an admin **"Close the Age"** button triggers it. Nav: **📚 Annals** added to the World group. Also enriched the **Field Manual with a "🎯 Strategy & Your First Days"** chapter adapted from the original Quick Start (grow-first, punch-down, soften-then-strike, mercs a tier below, spies wreck engines, clans). Specs updated (victory.md). 3 new server tests (89 total, first `lib/server` test); verified in-browser.
- **Intent-grouped nav + a proper World News feed**: the TopNav is now three clearly-divided groups — **You** (Command · Chronicle · Advisors) · **The World** (World News · Rankings · Clan · Forum) · **Help & Charter** (Field Manual · Steward/Charter, pushed right). The old flat "War Ledger" table (`/battles`) is reborn as **🌍 World News — the realm at war**, reusing the Chronicle's colourful tone-rows: a **"Wars Afoot"** panel listing active clan wars (deduped pairs, "X leads the slaughter by N of 200 kills"), then **"The Heralds"** — recent public battles narrated with flair ("Emberwatch storms the castle of Thornkeep — held the field"), mode-tinted (raid=amber, siege=crimson, revenge=blood, bombard=violet), with **clan tags ⟨Banner⟩** beside each combatant, a **⚔ clan-war badge**, losses/damage, and relative time. Chronicle (your story) and World News (the realm's story) stay separate scopes but share the look and cross-link. 86 tests pass; verified in-browser.
- **TopNav now carries the overview too + a colourful, time-stamped Chronicle**: Command View, Chronicle, and the Field Manual moved out of the sidebar into the horizontal TopNav (now three groups — 🏰 Court: Command/Chronicle/Advisors/Field Manual · 📜 World: Rankings/Ledger/Clan/Forum · 👑 premium), leaving the **sidebar to just The Realm + The War**. The **Chronicle reads like a scribe's ledger**: tone-coloured rows (`eventTone` → war/shadow/danger/growth/trade/clan left-border + `color-mix` tint), lightly flowery medieval phrasing ("Emberwatch fell upon us in raid", "Bread returns to the tables"), correct singular/plural, and **relative timestamps instead of turn numbers** ("just now" · "18m ago" · "3h 20m ago" · "2d 4h ago"). Times come from a new wall-clock stamp on inbox items (`InboxItem.at`, set in `pushInbox`) via `components/timeAgo.ts`, falling back to tick-math (1 turn = 10 min) for pre-stamp events; the Battle Ledger and Command View preview use it too. 86 tests pass; verified light & dark.
- **Nav split (short sidebar + horizontal TopNav) + low-resource warnings**: the left sidebar now holds only the **core loop** (Court: Command View/Field Manual/Chronicle · Realm: Buildings/Workers/Research/Market · War: Army/Siege/Attack/Spy); everything secondary moved to a new **horizontal `TopNav`** under the resource bar — Advisors, Rankings, War Ledger, Clan, Forum, and a **contextual premium entry** (shows "👑 Royal Charter" → /premium until owned, then "🪶 The Steward" → /steward, so only one nav slot; both pages kept — buy vs manage). The **resource bar now flags low stores**: food turns **amber** when net-negative with <12h runway and **red (pulsing)** when <2h or starving (title shows the runway), and the bulk cells go amber at zero. Verified in-browser light & dark (active highlighting, red food cell, moved pages still load). 86 tests pass.
- **Tutorial wired everywhere + advisor-sourced alerts + a real research tree**: (1) **Tooltips now teach** — the `Info` popover gained an optional clickable **"📜 Read the manual →"** deep-link (popover made interactive: hover-bridge + `pointer-events` on hover/focus), wired across units/attack-modes/buildings/research/actions via new `*_GUIDE` maps in `descriptions.ts` (verified: hovering a field name → clicking the link navigates to `/guide#grow`). (2) **The top red/amber banners now come from named advisors** — new `AdvisorAlerts` component replaces the inline starvation block: Starvation → *Treasurer Poll* (danger), scatter-risk → *General Vosk*, breached walls → *Marshal Aldric* (both amber `alert-warn`); each banner links **"Ask <Advisor> →"** (`/advisors#<key>`) and **"📜 Field Manual"**. (3) **Advisors point onward** — each councillor card now shows a **"📜 Field Manual: <chapter> →"** link, and the chamber intro explains the banners are the same voices. (4) **Research tree UI** — the flat card grid is now a tree: a **Collegium trunk** with a tier ladder (which field level each Collegium level unlocks), CSS connector lines down to three **discipline branches** (⚒ Economy / ⚔ War / 🗡 Shadow), and field **nodes** with a 5-segment **level pip-track** (owned/next/locked), gated "Study this" buttons, SHADOW tags, effect + progress. 86 tests pass; verified in-browser in light & dark.
- **Winning conditions surfaced + the Field Manual (in-game tutorial)**: new **"The Race to the Throne"** panel (`components/VictoryTracker.tsx`) headlines the Command View and Rankings — both victory paths (Grand Overlord / Clan) with plain-English rules, the reigning #1's two **hold-clocks as progress bars** + pop-floor status, and *your* standing (rank, points, gap to the crown). New **`/guide` Field Manual** page: anchored chapters (Winning the Era · How to Grow · Building an Army · Battle Strategies · Spies & Scouts · Clans · Defending the Realm) drawn from the specs, with a chip TOC. Wired a **📜 Field Manual** nav entry (The Court) and small **"how this works →" deep-links** (`components/LearnLink.tsx`) atop Buildings/Train/Research/Troops/Attack/Siege/Spy/Clan/Rankings, each to the matching chapter anchor. Also **more breathing room around the Command View portrait** (reusable `.portrait-frame`: padded frame, margin, race caption). 86 tests pass; verified in-browser (Command View race panel, guide chapters, attack deep-link).
- **Favicon**: PixelLab-generated heraldic shield with crossed golden swords, wired as `app/icon.png` (64), `app/favicon.ico` (16/32/48), and `app/apple-icon.png` (180) — Next.js App Router auto-links all three.
- **Ore is now the war-metal**: buildings no longer cost ore (the ore share of every cost band moved into stone; wood values unchanged, so build totals hold). In return, troop ore costs jumped — footman 40→90, archer 20→55, cavalry 40→130 ⚒️ each (×2 medium, ×4 heavy). Miners now feed the army, not the masons. Specs (buildings.md cost bands + baseCost examples + training table) updated; building cost cell hides zero resources; the Army page shows the per-unit ore cost. Tests updated (86 pass).
- **Benefits, next-level previews, plain-English, tooltip polish**: Buildings gained a "The next level brings…" column (plain-English `buildingUpgradeBenefit`: "20 → 40 farmer jobs", "shelters food 0 → 20,000 from raiders", "unlocks the Battering Ram", "wall defence +40% → +50%"); Research cards show a "Now +X% → +Y% at level N" effect line (`researchLevelEffect`). Verbose per-building/-field tooltips (`BUILDING_INFO`, `RESEARCH_INFO`) and expanded unit/action tips. **Acronyms replaced with plain English** everywhere — RP→"research points", XP→"experience", a/d→"you / them" / "attacker / defender", f/w/s/o→"food/wood/stone/ore". **Tooltip clipping fixed** — removed the panel-body overflow clip, tooltips now center over their mark, anchor left/right at row edges, and cap at min(340px, 78vw). Verified in-browser; 85 tests pass.
- **UX polish pass (tooltips, slider, pills, advisors, alerts, navbar)**: new reusable `Info` hover/focus tooltip (keyboard-reachable) + central `descriptions.ts` (units, actions, attack modes, tiers) wired into Buildings, Army, Workers, Siege, Attack, Command View. **Tax dropdown → live slider** (`TaxSlider`, shows gold/output trade-off, auto-submits). **Dropdowns → segmented `Pills`**: attack mode, troop tier (with costs), market resource. **Advisors rewritten** — each is now 2–3 specific, actionable sentences with real numbers. **Warning banners → proper alerts** (icon + title + explanation + CTA links; starving alert links to Bazaar/farmers). **Navbar clarified** — client `SideNav` with section subtitles (overview/economy/military), a one-line description under every item, native tooltips, and active-page highlighting. Verified in-browser; 85 tests pass.
- **UI scale-up + Siege Works defensive counters**: bumped base font (13→15px) and all chrome/table type, enlarged pixel art (Art default 48→60, race portraits →88), widened the frame; **lightened dense tables** everywhere (Buildings: 10 cols → 5 with a compact one-cell cost; Siege arsenal 7 → 5; Rankings dropped Title; War Ledger 7 → 5). Added the **🛡 The Ramparts** section to Siege Works showing all 5 **defensive counters** (Bill-hooks / Fork Poles / Boiling Oil / Hoardings / Counter-Engine) as art cards with what each blunts and installed/locked status — previously the counters had no visual home. **5 new PixelLab sprites** (64 total in `public/art/siege/`). Verified in-browser.
- **Bombard rework + building health model**: bombard no longer picks a target — it pounds the **walls first**, and once breached (≤50%) stray fire cracks **random town buildings** each round, weighted storages > production > Collegium (`resolveBombard` rewritten; verified in a real battle: walls −68%, then Ironhold ×3 / Counting House / Deepvein Mine / Collegium cracked). New per-building **integrity** (0.5–1.0 floor) replaces `storageIntegrity`: damaged storages shelter less, damaged producers yield less, a cracked Collegium researches slower (wired into tick + reports). `repairStorage`→`repairBuilding`; report `storageDamage`→`buildingDamage[]`; public ledger shows `buildingsHit`. Specs updated (combat/buildings/overview/architecture); 3 bombard + 2 integrity tests (85 total). CLI: health column + `repair <id>`.
- **Dark mode + light/dark toggle**: full CSS-variable theming (light = current parchment, dark = leather/gold), cookie-persisted, server-rendered (no flash), ☀️/🌙 toggle in the top bar. 90+ inline colors routed through theme vars; SVG price charts, admin, all pages themed. Verified in Chrome across buildings/command/market/research/siege/forum/attack in both themes.
- **Buildings page redesign + health bars**: the one monolithic table is now grouped panels (Production / Storage / Knowledge & Trade / Housing; Barracks & Tiers / Siege & Defence), each row with a **health bar** (`HealthBar` component) and a repairs panel that lists every damaged building. 
- **The War Ledger (public battle views)**: every battle now has a redacted public view (`publicBattle`) — who/mode/victor/rounds, **aggregate** troops lost per side, gear destroyed, wall/storage % — never composition, loot, or the narrated log (spies remain the intel path; spec'd in combat.md). New: `/battles` page (global last 100), `/empire/[id]` profile pages (race portrait, ladder facts, battle totals, recent battles) linked from Rankings + ledger rows, nav entry; API `GET /api/battles` + `GET /api/empire/:id`; CLI `battles [n]` + `profile <who>`. Verified in browser + CLI.
- **Detailed battle narration**: the combat log now tells every phase with real numbers — counter callouts ("Boiling Oil scalds our ram crews (−75%)"), siege volleys w/ wall %, defender engines answering, Counter-Engine kills, arrow volleys, cavalry charges, and the melee, each attributing losses per unit class ("defenders lose 1 footmen, 10 warriors"). CLI battle report gained the Butcher's Bill (per-class losses both sides), aftermath stamina/XP, gear lost, and storage-damage lines; the web report now shows siege gear lost + Counter-Engine kills (it already tabled losses/stamina/XP). 82 tests (new phase-narration regression).
- **Claude Code plugin + CLI ASCII art** (inspired by claude-arcade): the repo is now a plugin marketplace (`.claude-plugin/marketplace.json` + `claude-plugin/` — validated with `claude plugin validate`). `/woe` slash command = the court herald; `playing-war-of-empires` skill carries the full API reference, rules digest, opening-book strategy, and ASCII dashboard templates so Claude plays over plain curl (same realm token as web/CLI, server stays authoritative). CLI got ASCII art: castle-gate banner, crossed battle standards, victory trophy, defeat skull, founding flag.
- **Admin console** (`/admin`, hidden, behind `ADMIN_PASSWORD` — disabled when unset): banish/pardon (blocks cookie + realm token + all commands; empire stays in the world), royal resource grants (Chronicle-notified), force +1/+144/N turns (production-safe, unlike the dev clock), grant/revoke the Royal Charter. HMAC admin cookie, timing-safe password check. Verified in-browser end to end incl. CLI lockout while banned.
- **Login UX + realm tokens + terminal client** (no game changes): per-empire **realm token** (`woe_…`) — bearer auth for `cmd:*` + new JSON read endpoints (`POST /api/join`, `GET /api/state|rankings|market|battle/:id`). Login page: auto-skips when a session exists, 🎲 random-name dice, token re-entry replaces the open empire list (which is now dev-only); token shown in Command View ("Rule from the terminal"). **`cli/woe.mjs`** (`pnpm woe`): zero-dep, full-color ANSI terminal client — join/link, status dashboard, buildings + costs, train/equip/tax/rest/bank, research, rankings, attack-by-name w/ colored battle reports, market buy/sell, spy/scout, mercs/gear, REPL + one-shot. Verified end-to-end: founded an orc empire from the terminal, raided a browser-made empire, re-entered it in the browser via its CLI token.
- **Premium — the Royal Charter (Stripe) + the Steward** (`spec/premium.md`, new): $4.99 one-time via Stripe Checkout (webhook + success-redirect grant, idempotent); with no Stripe keys, /premium shows a built-in test terminal emulating Stripe's test cards — verified in-browser with 4242 4242 4242 4242 (success) and 4000…0002 (declined). Unlocks the **Steward** (engine-pure, runs each tick): build queue (strict FIFO, builds when affordable), research queue (auto-advances fields), standing orders ("once Drill Yard is built, train 1,000 warriors" — partial fulfillment across ticks). New pages /premium + /steward, queue buttons on Buildings/Collegium, 13 new engine tests (81 total). Fairness pillar: the Charter buys attention, never power.
- **UI reorganization**: Command View slimmed to overview + at-a-glance + chronicle preview; Buildings split into Civilian/Military tabs; new dedicated pages — **The Collegium** (/research, 10 field cards with emblems + progress bars), **The Advisors** (/advisors, four named councillors with portraits), **Siege Works** (/siege, arsenal with weapon art + foundry ladder + engineer training, moved out of Troops/Train/Buildings). Nav grouped into Court/Realm/War/World sections; ruler name + abdicate moved into the top bar; dev time controls boxed and labeled testing-only (auto-hidden in production).
- **25 more PixelLab assets** (59 total): 6 worker portraits (Train page), 10 research emblems, 4 advisor portraits, 5 siege engines.
- **A11y audit (Lighthouse 95)**: aria-labels swept onto all form controls, :focus-visible outlines, horizontal-scroll guards on dense tables.
- **Loot model finalized**: raids grab anything outside storage (never gold); the **castle attack** (siege) is for gold + spilled stores; **bombard caps storage damage at 50%** — it cracks storehouses open (overflow spills outside for the castle attack) but never levels them. Engine + specs + tests (68 passing).
- **Pixel art everywhere (PixelLab)**: 34 generated assets in `public/art/` — 21 buildings, 7 units, 6 race portraits — wired into Buildings, Train, Troops, Attack, Command View, and a race-picker on login.
- **Market price charts**: hourly price sampling per resource (2 weeks kept, survives in the world doc) rendered as parchment SVG line charts on the Bazaar page.
- **Chronicle page**: full event history + battle ledger with report links (new nav entry).
- **Economy rebalance (sim-driven, 2 iterations)**: gold 40 → **0.4 g/civ/turn** at 100% tax (anchor: ~29 g/civ/day at 50% — armies cost days of income to rebuild); merc upkeep 10 → 1 g/turn; resource score divisor 50 → 2,000. Treasury share of a pathological hoarder's score: 93% → 2–33%. Specs updated.
- **Supabase is live**: project `war-of-empires` (us-west-1) created via CLI, both migrations applied, keys in `.env.local`. The store persists the whole World as a versioned JSONB doc (`world_docs`) — verified end-to-end (empire created in browser → row in cloud Postgres). File store remains the no-env fallback.

## Top open items (details in todo.md)
1. Combat tuning: lethality k=2 (1–3-round battles) and ~19% attacker win rate in even fights — decide if intended.
2. Decompose the world doc into the normalized 0001 tables; Supabase Auth (cookie stands in); Realtime (refresh stands in).
3. Vercel deploy — unblocked; needs your Vercel login + env vars.
4. Clan v1 leftovers (invite/kick/appoint UI, diplomacy, 5-founder rule, clan-bombard revenge); era auto-transition.
5. Provisional decisions to confirm: shield-drop-on-attack, spy-block-vs-protected.

## Designed (spec/) — unchanged summary
- **Economy** — tax dial (0–100%), production trade-off, mercenary-only upkeep; food = population upkeep (0.1/person/turn), starvation freezes the empire
- **Buildings** — 13 levelled civilian + counted housing/barracks; pop growth 1→100/day; cost ratio bands; storage = 20k × level protected; instant builds
- **Research** — 10 fields × 5 levels, exponential costs (×5/level), specialization forced
- **Market** — anonymous Grand Bazaar, player-set asks, cheapest-first fills, caravans 1k × Market Square level
- **Combat** — raid/siege/revenge/bombard (10 action turns, instantaneous); 4-phase battles; wall integrity; scattering (<30% troops, exempt <500 pop); XP bands (refusal ≥75% up, −5 below 50%); ±10% luck
- **Siege** — War Foundry 1–10, five offense/counter pairs, counters −75%
- **Espionage** — 5 Tradecraft ops, lodge-level catch gating, caught spies executed, ±20% luck
- **Clans** — 5 roles; storage (3× cap) → hall (tax shelter ≤50%) → wonder (−10/20/30%); wars (+100% dmg, +200 net kills → XP transfer + tribute, 48h truce); churn (forfeit, 48h, 2/era)
- **Victory** — eras named after winners; Overlord (72h cum + 12h straight, 10k floor) / Clan (150k floor); visible-empire ranking
- **Races** — sum-zero table, Humans baseline, Gnolls spymasters
- **Premium** — Royal Charter ($4.99 one-time, Stripe) → the Steward: build/research queues + standing orders; buys attention, never power
- **Engineering** — Next.js on Vercel + Supabase + pure-TS engine (see architecture.md, incl. dev-mode stand-ins note)
